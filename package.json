{
  "scripts": {
    "test": "vitest run",
    "release-notes": "npx --yes tsx src/release-notes.ts vitest-output.json release-notes.md"
  },
  "dependencies": {
    "vitest": "^4.1.7",
    "zx": "lite"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
